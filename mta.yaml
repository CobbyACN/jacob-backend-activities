_schema-version: 3.3.0
ID: BackendActDay2
description: A simple CAP project.
version: 1.0.0
modules:
- name: BackendActDay2-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: BackendActDay2-auth
  - name: BackendActDay2-db
  - name: BackendActDay2-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: BackendActDay2-db-deployer
  type: hdb
  path: db
  requires:
  - name: BackendActDay2-db
  parameters:
    buildpack: nodejs_buildpack
- name: BackendActDay2-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: BackendActDay2-auth
  - name: srv-api
  - name: BackendActDay2-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - febootcamp.zip
      name: febootcamp
      target-path: app/
- name: BackendActDay2-destination-content
  type: com.sap.application.content
  requires:
  - name: BackendActDay2-destination
    parameters:
      content-target: true
  - name: BackendActDay2-html5-repo-host
    parameters:
      service-key:
        name: BackendActDay2-html5-repo-host-key
  - name: BackendActDay2-auth
    parameters:
      service-key:
        name: BackendActDay2-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: FEBOOTCAMP_BackendActDay2_html5_repo_host
          ServiceKeyName: BackendActDay2-html5-repo-host-key
          sap.cloud.service: FEBOOTCAMP
        - Authentication: OAuth2UserTokenExchange
          Name: FEBOOTCAMP_BackendActDay2_auth
          ServiceInstanceName: BackendActDay2-auth
          ServiceKeyName: BackendActDay2-auth-key
          sap.cloud.service: FEBOOTCAMP
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: febootcamp
  type: html5
  path: app/febootcamp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: BackendActDay2-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: BackendActDay2-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: BackendActDay2-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: BackendActDay2-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: BackendActDay2-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
